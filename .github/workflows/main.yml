name: Deploy Node App for Multiple Domains

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: SSH and Deploy
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_IP }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd ~/kushal-finance-backend
            sudo git pull https://dusksoni:GITHUB_PAT_REMOVED@github.com/dusksoni/kushal-finance-backend.git main
            sudo npm install -f


            # ===== API1.COM =====
    
            sudo cp ~/backend-envs/kushalfinance.env .env
            sudo npx prisma migrate deploy
            sudo npx prisma db seed
            set +e  # disable exit on error
            pm2 delete kushalfinance || true
            set -e  # re-enable error stop for rest
            PORT=3001 pm2 start index.js --name kushalfinance

            # # ===== API2.COM =====
            # cp ~/envs/api2.env .env
            # pm2 delete api2 || true
            # PORT=4002 pm2 start index.js --name api2 --log ~/logs/api2.log

            pm2 save